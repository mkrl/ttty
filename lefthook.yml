pre-push:
  parallel: true
    tsc:
      tags: typescript
      run: tsc --noEmit --incremental false

pre-commit:
   parallel: true
   commands:
     eslint:
       glob: "*.{ts}"
       run: yarn eslint {staged_files}
